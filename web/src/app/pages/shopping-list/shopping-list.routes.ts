import { Routes } from '@angular/router';
import { ShoppingListComponent } from './shopping-list.component';

export const INCOMING_ROUTES: Routes = [
  { path: '', component: ShoppingListComponent },
];
